/*
Create scala program (Company.scala) that contains the following starter code.
object Company extends App {
}
Create a class called Employee in the same file. 
The class has one string variable called name. 
The class contains a method paySalary(amount: Int) and 
it prints the text "Salary paid: {amount}". 
verride also the toString method and it should print "Hello {name}"

Create one employee object using the name "Lisa Smith" and 
call the paySalary method passing 2000 as an argument. 
Finally, call the toString method.
Example output:
Salary paid: 2000
Hello Lisa Smith
*/